# Stacks network settings - contains mnemonic seed phrases
# (.example templates are committed; copy them to the real filenames locally)
settings/Testnet.toml
settings/Mainnet.toml
settings/Devnet.toml
settings/Simnet.toml

# Deployment plans may contain sensitive account info
deployments/default.testnet-plan.yaml
deployments/default.mainnet-plan.yaml

# Regenerated by the vitest simnet harness on every test run
deployments/default.simnet-plan.yaml
